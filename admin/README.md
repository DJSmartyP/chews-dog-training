# Admin placeholder

No admin UI is included in this prototype. The data contracts and content-provider boundary are intentionally in place so a later Firebase Authentication + Firestore + Storage admin can manage classes, venues, events, gallery, team and site settings without changing the public design.
